from setuptools import setup, find_packages

setup(
    name="qwen3_moe_fused",
    version="0.1",
    packages=find_packages(),
    # # 添加依赖项
    # install_requires=[
    #     # 列出您的依赖项，例如
    #     # "torch>=1.10.0",
    # ],
)